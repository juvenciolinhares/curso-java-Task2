package modificadoresDeAcesso;

public class ModificadoresDeAcesso {


	/*  private; // restringe o acesso a membros da propria classe;(pouco utilizado).
		package; //acessivel a todos os membros dentro do mesmo pacote;
		protected; // restringe o acesso aos filhos de uma classe;
		public; // acessivel em qualquer lugar;

	 * os modificadores servem para classes, atributos, metodos
	 * o padrão pra classes é public ou package.
	 * 
	 * private é mais usado para situações dentro da propria classe
	 * quando trabalho com um recurso auxilira dentro de uma classe.
	 * 
	 * package pra classes:
	 * quando essa classe e metodos compuserem uma serie de classes
	 *  que realizam uma função especifica
	 *  exemplo: calculoes estatisticos.
	 *  aplicação bancarias: módulo tranferencia, módulo pagamento, modulo deposito
	 */


}
